server:
  port: 8000
  servlet:
    context-path: /daily

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/daily?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root

#mybatis:
#  type-aliases-package: cn.dodaily.dailyserver